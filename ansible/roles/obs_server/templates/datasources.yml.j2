apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{prometheus_host}:{{ prometheus_port }}
    isDefault: true
    editable: true
  - name: Loki
    type: loki
    access: proxy
    url: http://{loki_host}:{{ loki_http_port }}
    editable: true
