vrrp_instance VRRP1 {
    state BACKUP
    interface {{ interface_name }}
    virtual_router_id 51
    priority {{ keepalived_priority }}
    advert_int 1

    authentication {
        auth_type PASS
        auth_pass secret
    }

    virtual_ipaddress {
        {{ vip_address }}
    }
}